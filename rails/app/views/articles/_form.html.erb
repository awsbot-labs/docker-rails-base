                          <%= form_for(@article, html: { class: "form-horizontal push-10-t push-10" } )  do |f| %>
                            <%= f.hidden_field :user_id, value: current_user.id  %>
                            <% if @article.errors.any? %>
                              <div id="error_explanation">
                                <h2><%= pluralize(@article.errors.count, "error") %> prohibited this article from being saved:</h2>

                                <ul>
                                <% @article.errors.full_messages.each do |message| %>
                                  <li><%= message %></li>
                                <% end %>
                                </ul>
                              </div>
                            <% end %>
                            <div class="row">
                                <div class="col-sm-12">
                                    <div class="form-group">
                                        <div class="col-xs-12">
                                            <%= f.label :title %><br>
                                            <%= f.text_field :title, class: "form-control input-lg", placeholder: "My blog title.." %>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <div class="row">
                                <div class="col-sm-12">
                                    <div class="form-group">
                                        <div class="col-xs-12">
                                            <%= f.label :body %><br>
                                            <%= f.text_area :body, class: "form-control input-lg", placeholder: "Enter a few details about yourself..", rows: "22" %>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <div class="row">
                                <div class="col-sm-7">
                                    <div class="form-group">
                                      <div class="col-xs-12">
                                        <%= f.submit class: "btn btn-warning" %>
                                      </div>
                                  </div>
                               </div>
                            </div>
                        <% end %>